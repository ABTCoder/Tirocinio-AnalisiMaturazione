function [ellipses]  = get_ellipses(im)

close all;

% parameters
Tac = 165;
Tr = 0.6;
specified_polarity = 0;

disp("[MATLAB] detecting ellipses...")
[ellipses, ~, posi] = ellipseDetectionByArcSupportLSs(im, Tac, Tr, specified_polarity);


end